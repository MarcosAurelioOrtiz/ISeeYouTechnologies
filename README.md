# Instalación de servidor

Directorio de trabajo:

```bash
cd node/
```

Instalacion de dependencias:

```bash
npm install
```

Ejecución:

```bash
npm run server
```

# Instalación de frontend

Directorio de trabajo:

```bash
cd frontend/
```

Instalacion de dependencias:

```bash
npm install
```

Ejecución:

```bash
npm start
```
